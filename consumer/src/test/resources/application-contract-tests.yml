logging:
  level:
    root: warn
  pattern:
    level: '[%X{testCase}/%X{traceId} %-5p [%t] %C{2} - %m%n'

feign:
  client:
    config:
      default:
        connectTimeout: 5000
        readTimeout: 5000
        loggerLevel: full

providerService:
  url: http://localhost:8091

marketingService:
  url: http://localhost:8091